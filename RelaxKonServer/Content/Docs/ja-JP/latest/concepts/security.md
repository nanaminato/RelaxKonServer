---
title: セキュリティモデル
description: RelaxKonOS の ID、権限、昇格の境界。
category: 概念
order: 64
---

# セキュリティモデル

RelaxKonOS は独自の ID・権限体系を作らず、**ID とファイル権限をホスト OS に委任**します。

## 3 つの柱

### 1. ID はホストへ委任

- Windows：ネイティブの `LogonUser` API（ローカルアカウントとドメインアカウントに対応）
- Linux：PAM + NSS
- ホストが資格情報を確認した後にのみトークンを発行し、**パスワードを保存しません**

### 2. 権限はホストを継承

- ファイル操作はサインインユーザーのホスト OS 権限で実行
- 独自の ACL 層を持たない
- 権限外の要求はホスト OS が拒否

### 3. 昇格は分離

- 特権操作は専用ヘルパープロセスで実行
- 契約は狭く、監査対象
- 破壊的な操作には明示的な確認が必要
- sudo・UFW・systemd などホストの仕組みを優先的に再利用

## 多層防御

| レイヤー | 対策 |
| --- | --- |
| 転送 | HTTPS、JWT |
| プロトコル | 明示的な契約、入力検証 |
| サーバー | パス正規化、ディレクトリトラバーサル対策、統一エラー処理 |
| 描画 | Markdown のエスケープ、埋め込み HTML を既定で信頼しない |

## 関連ドキュメント

- [永続化](/docs/ja-JP/latest/concepts/persistence)
- [セッションとデバイス](/docs/ja-JP/latest/concepts/session)
