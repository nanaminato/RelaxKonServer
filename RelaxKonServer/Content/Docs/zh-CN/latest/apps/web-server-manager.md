---
title: Web Server 管理器
description: 发现本机 Web Server，经管理员确认后集成或托管，并以可验证的事务方式管理站点。
category: 应用程序
order: 41
---

# Web Server 管理器

Web Server 管理器是**入口与调度层**：它发现本机上的 Web Server 实例并按 Provider 路由，Nginx 是第一个完整实现的 Provider。新增其他 Provider 时，调用方不需要改变。

## 概览

RelaxKonOS 不被设计成「自带 Nginx」，也不是一个 Nginx 管理面板。它的定位是：**能够发现本机 Web Server，并根据用户授权选择监控、集成或托管。**

## 两种管理模式

| 模式 | 含义 | 能力边界 |
| --- | --- | --- |
| 集成（Integrated） | 与现有 Web Server 集成，但不拥有它 | 只管理 RelaxKonOS 自己创建的配置片段，不负责安装、升级、卸载 |
| 托管（Managed） | 由 RelaxKonOS 安装并完整管理 | 安装、升级、启停、配置、站点、卸载 |

发现到未受管的 Web Server 时，它只是一个**候选项**：RelaxKonOS 不会自动修改其配置。只有管理员明确确认后才进入集成模式。

## 适用场景

- 服务器上已经有 Nginx，希望在不接管原有配置的前提下管理新站点。
- 需要为某个域名配置静态前端、单页应用回退与多条反向代理路径。
- 需要让证书签发与部署解耦：证书管理器不知道 Nginx 的存在。

## 当前能力

- 发现：从 PATH、常见安装路径、系统服务与已运行进程等多个来源探测候选实例。
- 只读状态：版本、可执行文件、主配置与配置目录。
- 配置事务：生成配置 → 备份 → 写临时文件 → 校验 → 提交 → 重载 → 失败回滚。
- 站点模型：静态根目录与代理路由**不是互斥类型**；纯静态站点只填根目录，纯代理站点只用一条覆盖全站的根路由，整合站点可以两者并存。
- 证书绑定：站点可绑定证书管理器中已有的证书；私钥仍只保留在服务器本地。
- 续期后自动重载：证书续期成功后，仅对 RelaxKonOS 管理的、引用该证书的站点执行一次安全重载，不修改站点配置。
- HTTP-01 集成：已有 Web Server 占用 80 端口时，只负责暴露挑战路径，RelaxKonOS 不抢占 80。
- 权限授予：Linux 上可为静态目录显式授予 worker 读取权限（仅读取与穿越，不改所有者、不给写入）。

## 权限与安全

- 这是面向单台服务器管理员的**管理员模式**功能，不使用细粒度的用户 / 工作区授权。实例、站点与配置快照都是宿主机全局资源。
- 发现与只读状态在进程可读时即可运行；集成、安装、写配置、重载、启停与证书部署都要求 RelaxKonOS 以足够权限运行，否则返回稳定问题码。
- 客户端不收集 sudo / UAC / 服务账户口令，也不把请求参数拼接成 shell 命令；高权限执行器只接受本模块定义的结构化操作与允许的路径。
- 上游地址不是可任意写入的 URI：拒绝凭据、控制字符、未知协议与未声明端口，并在解析后再次校验地址，避免把站点表单变成内网探测接口。
- 配置事务在每个实例上串行执行；外部修改与快照不一致时中止并要求重新读取，不覆盖用户变更。
- 删除站点或卸载只删除带所有权标记且校验一致的文件，绝不递归删除用户目录。

## 平台差异

- 目标平台为 **Ubuntu 24.04 LTS** 与 **Windows Server 2016 及以上**。
- Linux 通过系统服务或原生命令控制 Nginx；Windows 下由 RelaxKonOS 服务负责受管 Nginx 的进程生命周期。

## 已知限制

- **Nginx 集成当前是第一阶段范围**：安装、升级、卸载以及配置事务与站点管理已落地，其余 Provider 尚未实现。
- IIS、Apache、Caddy 只保留了抽象边界，**没有实现**，界面不会宣称可用。
- 证书的签发与自动续期后触发重载仍可在后续阶段进一步自动化；当前不承诺未实现的 Provider 能力。
- 复合站点配置、目录授权与敏感路径限制都按当前实现如实呈现，不提前承诺。

## 相关文档

- [证书管理器](/docs/zh-CN/latest/apps/certificate-manager)
- [应用部署](/docs/zh-CN/latest/apps/application-deployments)
- [安全模型](/docs/zh-CN/latest/concepts/security)
